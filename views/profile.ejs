<!DOCTYPE html>
<html lang="en">

  <body>
    <div>
      <h1>Profile</h1>
      <form class="form-inline" action="/update/" method="POST">
      <h3>
        <% for(let data in user) { %>
          <label>Name:</label> <%= user[data].name %>
          <input type="text" name="updateName" placeholder="Enter new name" style="width: 300px; margin: 1em">
          <button type="submit" class="btn btn-primary">Update</button>
          <br>
          <label>Email:</label> <%= information[info].email %>
          <% } %>
      </h3>
      </form>
    </div>
  </body>
